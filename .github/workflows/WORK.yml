name: Push to AWS ECR

on:
  push:
    branches:
      - dev

env:
  AWS_REGION: 'us-east-1' # Set your AWS region here
  ECR_REPOSITORY: 'repo'
  IMAGE_TAG: 'latest'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, tag, and push image to Amazon ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG


      - name: Fill in the new image ID in the ECS task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: my-vpc:1
          container-name: repo # 请替换为您容器的实际名称
          image: ${{ steps.login-ecr.outputs.registry }}/repo:latest

      - name: Deploy Image to ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          service: my-service  # 替换为您的服务名称
          cluster: my-cluster  # 替换为您的集群名称
          task-definition: ${{ steps.task-def.outputs.task-definition }}



